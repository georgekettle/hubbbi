<%= render "components/headers/header", header: @group_member.user.full_name_or_email, subheader: "Profile" %>

<%= render "shared/container", classes: "p-4 flex flex-col items-center" do %>
  <div class="mb-2">
    <%= render "components/avatar", user: @group_member.user, size: "24" %>
  </div>
  <h2 class="text-xl tracking-tight font-medium"><%= @group_member.user.full_name_or_email %></h2>
  <p class="text-sm text-gray-400">A little description about myself</p>
<% end %>
