<% editable ||= false %>

<div class="section">
  <%= render "components/headers/section_header", title: "Content", icon:"library" do %>
    <% if editable %>
      <div class="flex items-center">
        <% if policy(section.page).update? %>
          <%= render "sections/options_dropdown", section: section %>
          <div class="icon-btn faded-highlight">
            <%= render "icons/handle", classes: "h-4 w-4" %>
          </div>
        <% end %>
      </div>
    <% end %>
  <% end %>

  <div class="-mx-1 flex flex-wrap">
    <% page_references.each do |ref| %>
      <div class="p-1 w-1/2">
        <%= render "page_references/page_reference", page_reference: ref %>
      </div>
    <% end %>
  </div>
</div>


