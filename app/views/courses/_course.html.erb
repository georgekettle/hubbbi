<%= link_to course, class: "mb-2 block" do %>
  <% if course.cover.attached? %>
    <%= render "components/card", title: course.title, subtitle: pluralize(course.course_members.count, "member"), image: course.cover %>
  <% else %>
    <%= render "components/card", title: course.title, subtitle: pluralize(course.course_members.count, "member"), no_shadow: true %>
  <% end %>
<% end %>
