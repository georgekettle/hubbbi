<% size ||= 12 %>

<% image_path = group.cover && group.cover.key ? cl_image_path(group.cover.key) : asset_path('default_cover.svg') %>
<div class="rounded-md h-<%= size %> w-<%= size %> overflow-hidden border border-white" style="background-size: cover; background-image: url(<%= image_path %>)"></div>
